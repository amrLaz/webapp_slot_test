name: 'Terraform'

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v1
        
    
    - name: Terraform Format
      id: fmt
      run: terraform fmt -check

    - name: Terraform Init
      id: init
      run: |
        cd terraform/
        terraform init

    - name: Terraform Validate
      id: validate
      run: |
        cd terraform/
        terraform validate -no-color

    - name: Login as LZ Builder
      run: az login --service-principal -u $ARM_CLIENT_ID -p $ARM_CLIENT_SECRET -t $ARM_TENANT_ID
      env:
        ARM_CLIENT_ID: "85b43cdc-c798-4758-9184-104d7d132d67"
        ARM_CLIENT_SECRET: "tgY8Q~rvz71qJJ~djUupVD_HCRHdUSk6Dt8WhcGC"
        ARM_TENANT_ID: "b0645364-eba2-4775-843e-4f68023d2f2c"

    - name: Terraform Plan
      id: plan
      if: github.event_name == 'pull_request'
      run: |
        cd terraform/
        terraform plan -no-color -input=false
      continue-on-error: true

    - name: Terraform Plan Status
      if: steps.plan.outcome == 'failure'
      run: exit 1

    - name: Terraform Apply
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      run: |
        cd terraform/
        terraform apply -auto-approve -input=false